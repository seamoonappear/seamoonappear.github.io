# Gemfile

source "https://rubygems.org"

gem "jekyll-theme-chirpy", "~> 6.3" # 请检查并使用最新的 Chirpy 主题版本

group :test do
  gem "html-proofer", "~> 3.18"
end

# 如果 GitHub Pages 有指定版本的 jekyll，可能需要取消注释并指定版本
# gem "github-pages", group: :jekyll_plugins

# 以下行可帮助在 Windows 等非 Linux 环境下生成与 GitHub Pages 兼容的依赖列表
# bundle lock --add-platform x86_64-linux